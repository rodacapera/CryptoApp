import Crypto from '../../../models/Crypto';
import {HomeScreenNavigationProp} from '../../../navigation/types/navigationTypes';

interface ListButtonTypes {
  navigation: HomeScreenNavigationProp;
  item: Crypto;
}
export type {ListButtonTypes};
